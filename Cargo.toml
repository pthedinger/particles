[package]
name = "particles"
version = "0.1.0"
edition = "2021"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3

[dependencies]
bevy = { version = "0.14.2", features = ["dynamic_linking"] }
bevy_pixel_buffer = "0.8.1"
bevy-inspector-egui = "0.27.0"
rand = "0.8.5"
